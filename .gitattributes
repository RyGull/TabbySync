# merge.js and the crypto tests contain deliberate NUL characters (used as
# key delimiters and as test input). Without this, git detects them as binary
# and stops producing readable diffs for them.
*.js text
*.json text
*.html text
*.css text
*.md text
