{
  "name": "tabbysync",
  "version": "1.3.4",
  "private": true,
  "type": "module",
  "description": "Test harness for the TabbySync extension. The extension itself does not read this file and has no dependencies.",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
