The extension requests no host access at install time. When the user saves a sync destination, it asks for access to that one address — their own server, api.github.com, or api.jsonbin.io — through the browser's own permission prompt, which names the site. It is declared under optional_host_permissions and limited to https:// (plus http://localhost for local testing), because the access code travels with every request.
