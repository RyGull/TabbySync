Reads and writes the user's bookmarks. This is the data one half of the extension synchronises: it reads the bookmark tree to send it to the user's chosen destination, and applies incoming changes from their other computers.
